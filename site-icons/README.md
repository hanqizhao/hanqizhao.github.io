Drop one of these files into this folder to enable the browser tab icon automatically:

- favicon.ico
- favicon.png
- favicon.svg

The loader in index.html checks this folder on startup and uses the first matching file it finds.