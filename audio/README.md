Place your licensed background music files here as outer-bgm.ogg and outer-bgm.m4a.

Expected public path:
- /audio/outer-bgm.ogg
- /audio/outer-bgm.m4a

The outer site tries the OGG file first and falls back to the M4A file if needed.
If you want different filenames, update BGM_CONFIG.sources in main.js.